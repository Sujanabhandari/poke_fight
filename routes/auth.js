var express = require("express");
var authRouter = express.Router();

// const {aythenticate_self} = require("../controllers/auth");

// authRouter.route('/register').post(aythenticate_self);

module.exports = authRouter;